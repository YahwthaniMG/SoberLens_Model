# Dependencias para el proyecto de detección de ebriedad
# Instalar con: pip install -r requirements.txt

# Descarga de videos de YouTube
yt-dlp

# Procesamiento de video e imagen
opencv-python>=4.8.0
numpy>=1.24.0

# Detección de rostros
# Opción 1: MTCNN (más preciso, un poco más lento)
mtcnn>=0.1.1
tensorflow>=2.13.0

# Opción 2: MediaPipe (más rápido, buena precisión) - Recomendado para Windows
mediapipe>=0.10.9

# Utilidades
tqdm>=4.65.0
pandas>=2.0.0